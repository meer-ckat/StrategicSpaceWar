# 최적화 기록 — 2026-08-23

SUPERRADIANCE 한 세션 분량의 성능 작업. 무엇이 느렸고, 왜 그랬고, 무엇을 바꿨고,
**무엇을 잘못 짚었다가 되돌렸는지**까지 남긴다. 마지막 것이 제일 값어치 있다.

---

## 요약

시작은 "destroyer 두 척 소환에 `ArmorSkin.Start`가 CPU의 57.7%, 1,190ms"였고,
끝은 "확실히 빨라졌다"였다. 그 사이에 한 일을 한 줄로 줄이면:

> **계산이 느린 자리는 거의 없었다. 느린 것은 전부 "매번 새로 만드는 것"과
> "안 봐도 되는 것을 보는 것"이었다.**

그래서 이 문서의 대부분은 알고리즘 교체가 아니라 **캐시·평면 배열·도장(stamp)**
이야기다. 알고리즘을 바꾼 자리는 셋뿐이다(링 검사, 후면 실루엣 flood, 파편 wave).

---

## 0. 먼저 한 일: 안 보이면 못 고친다

첫 프로파일은 `TickManager 33.5%`, 그 안은 전부 `Self`였다. Unity 프로파일러는
마커 없는 관리 코드를 부모의 Self로 뭉갠다 — **범인이 구조적으로 안 보인다.**

`ProfilerMarker`를 박아 이름을 붙였다:

- `TickManager.Early / Physics2D / Late`
- `Tick.<타입>` — 리스너 타입별 자동 (Ship / Gun / Hulk / Projectile / Armor …)
- `Ship.Split / Ram / Drive / Aim / Atmosphere / Watch`
- `Ram.Gather / Sweep / Conduct`, `Spall.Burst / Commit`, `TraceWorld.Build`,
  `Armor.KillOrphans / DamageEvenly`

릴리스에서 no-op이라 영구 장착. 이후 모든 결정이 이 이름들 위에서 내려졌다.

> **교훈**: "TickManager가 33% 먹는다"는 정보가 아니다. 마커를 박기 전까지는
> 딥 프로파일로 추측하고 있었고, 딥 프로파일은 잔호출을 뻥튀기해 엉뚱한 곳을
> 가리켰다.

---

## 1. 격자와 BFS — 평면 배열과 도장

### 링 검사 (알고리즘 교체)

판이 죽을 때마다 전체 선체 8방향 BFS가 돌았다. 연사 중에는 사실상 매 틱.

`ShipGrid.RemovalMightSplit` — 죽는 칸의 살아 있는 8이웃이 3×3 링 안에서 한
덩어리면 **그 죽음은 선체를 못 가른다.** 그러면 BFS 자체를 안 돈다. 외판 표면
피격 대부분이 여기서 끝난다.

비대칭이 요점이다: `false`("절대 못 가른다")가 틀리면 시뮬 버그, `true`("몰라서
BFS에 묻는다")가 틀리면 헛BFS 한 번. 그래서 링 밖으로 돌아 이어지는 ㄷ자 모양은
`true`가 정답이다. 그리고 **선체와 같은 8방향으로 세야 한다** — 4방향이면 L자
모서리의 대각 연결을 놓쳐 매번 헛BFS다.

### 전부 int 인덱스로

| BFS | 방문/마스크 | 큐 |
|---|---|---|
| `BuildStructure` (선체 8방향) | `bool[]` 정적 | `int[]` head/tail 정적 |
| `MarkExterior` (우주/실내 flood) | `bool[]` | `int[]` |
| `BuildRooms` (방 4방향) | `bool[]` | `int[]` |
| `SplitRear` (후면 다중소스) | `bool[]` + `int[]` 정적 | `int[]` 정적 |
| `FloodOutside` (후면 실루엣) | 마스크 자체에 도장 | `int[]` |
| `RamImpact.Conduct` (충격 전도) | `Armor.ConductStamp` 도장 | `Armor[]` head/tail |

`HashSet<Vector2Int>`는 칸·이웃마다 해싱하고 `Queue<T>`는 버전 검사와 용량 조정을
한다. BFS는 칸당 최대 1회 입큐라 **되감기(wrap)가 필요 없어서** `int[] + head/tail`이
`Queue<int>`보다 항상 낫다.

`HullStructure._alive` 장부도 `HashSet` → 평면 `bool[]` + 카운트로. 파단 BFS의
입력이 정확히 그 모양이라 **매 BFS마다 하던 마스크 복사 자체가 사라졌다.**

### 도장(stamp)이라는 패턴

집합 대신 정수 하나:

```csharp
// 파면마다 새 번호를 쓰므로 지울 일이 없다
int stamp = ++_conductStamp;
if (neighbour.ConductStamp == stamp) continue;   // 방문 검사 = int 비교
neighbour.ConductStamp = stamp;                  // 방문 표시
```

`Conduct`(전도)와 `Punch`(충각 스윕)가 **각자 자기 도장 필드를 든다.** 한 필드를
나눠 쓰면 안 된다 — 충각이 판을 때리면 그 자리에서 유폭이 나고 전도가 도장을 새
번호로 덮으므로, 돌아온 스윕이 이미 센 판을 다시 세고 **접촉 판 수가 부풀어
뾰족하게 댄 충각이 넓게 댄 것으로 계산된다.**

---

## 2. 충각 — 매 틱 전체를 보던 것

`Ship.Ram`이 항상 상위였다. 넷을 걷어냈다.

**① 반경 캐시** — `FarthestReach`가 매 틱 콜라이더 300개의 `bounds`(네이티브)를
훑었다. 조기 리턴보다 앞이라 **가만히 떠 있는 잔해도** 그 값을 냈다.

캐시 값은 "피벗까지 거리 + 피벗–AABB중심 + 반대각"의 **자세 불변 상한**이다.
처음엔 `bounds.center - 질량중심`으로 쟀는데, 그러면 **매 틱 도는 포탑**(offset이
있어 bounds.center가 움직인다)에서 상한이 깨진다. 콜라이더 수로 무효화한다 —
수리는 HP만 돌리고 콜라이더를 안 만드니 안 썩는다.

**② 브로드페이즈** — 사거리 안에 남의 몸이 없으면 스윕 자체를 생략. 포격전
거리에서 충각이 float 비교 몇 번으로 끝난다. 몸 스냅샷은 틱당 1회라 O(N²)이 O(N).

**③ 선별 캐스트** — `Rigidbody2D.Cast`는 내 콜라이더 300개를 **전부** 스윕한다
(`ColliderCastAll` 16%). 남의 몸 근처의 앞면 몇 장만 골라 `collider.Cast`하고
거리순 정렬로 합친다. 선별 기준은 콜라이더의 몸 로컬 피벗 캐시라 네이티브 0회.

**④ 전도 간선** — 간선마다 `transform.position` + `SameBodyAs`의 `transform.parent`
2회 = 네이티브 3회였다. `Armor.CellLocal`(로컬 위치 캐시, 재부모화에도 불변)과
`CachedBody` 참조 비교로 **간선당 0회**. 감쇠도
`along^x · across^y = exp(x·ln a + y·ln b)`로 `Pow` 2회 → `Exp` 1회.

축을 몸 로컬로 돌릴 때 함정이 있다: `InverseTransformDirection`은 scale을 무시하므로
**반전 함선(localScale.x = -1)의 부호를 손으로 마저 적용**해야 한다. 안 하면
거울상 배에서 감쇠 띠가 엉뚱한 축으로 돈다.

---

## 3. 그림 — CPU 굽기를 GPU로

### 판 (`ArmorSkin` → `PlateSkin.shader`)

판마다 96ppu 전 픽셀을 CPU가 굽고 있었다(소환 스파이크의 57.7%). 단계적으로:

1. `OverlapPoint`(픽셀당 물리 질의) → BoxCollider2D는 **로컬 사각형 산술**
2. `GetPixelBilinear`(픽셀당 관리 호출) → 함선당 픽셀 사본 1벌 + 수제 쌍선형
3. 시간분할(프레임당 24장)
4. **그리고 전부 버렸다** — 셰이더가 배 그림 + 6×6 손상 마스크 + 실루엣 마스크를
   조합한다. CPU 몫은 **피해 시 마스크 36바이트**뿐

### 후면 (`BackPlateView` → `RearSkin.shader`)

`DamageRear` 한 번에 맵 전체(40만 픽셀 + `GetPixelBilinear`)를 다시 구웠다.
bbox → 칸 단위 더티 → 링+커서 더티를 거쳐 결국 여기도 셰이더로 갔다. 마스크
셋(칸 수명 / worthy·경계 / 실루엣)을 프래그먼트가 조합하고, 뜯긴 가장자리는
**이웃 4칸 탭으로 셰이더가 직접 계산**한다.

### 후면 실루엣 — 모양의 출처를 격자에서 기하로 (알고리즘 교체)

증상: 함교 천장이 뚫려 우주가 비쳤다.

원인은 규칙 둘이 반대로 틀린 것이었다. 후면은 칸 단위(1m 정사각형)인데 발자국
크롭은 "우주에 닿은 경계 칸"에만 걸었다. 경사 지붕 칸은 **경계 칸이면서 아래가
방**이라 크롭이 그 칸의 안쪽 절반까지 잘라냈다. 반대로 큰 경사판이 자기 칸 밖을
덮으면 그 칸은 격자상 `Exterior`라 후면이 아예 없었다.

해결: **판 폴리곤의 합집합을 16배 해상도로 굽고 바깥에서 flood** — 닿지 않은 곳이
후면이다. `MarkExterior`의 "밀폐 주머니가 실내다"를 서브셀에서 한 번 더 도는
것이고, 다른 점은 벽이 1m 칸이 아니라 **판의 실제 폴리곤**이라는 것뿐이다.

- 판이 하나도 없는 방 한가운데도 후면이다(flood가 못 닿는 주머니라서)
- 지붕 칸의 바깥 절반은 우주와 이어져 깎인다 → 실루엣이 판 대각선을 따라간다
- 유일한 위험은 **이음매 누수**: 인접한 두 판이 16px 격자에서 1픽셀 벌어지면
  주머니 하나가 통째로 우주가 된다. 2×2 보수 래스터와 "판 없는 실내 칸은 칸
  전체로 채운다"가 그 보험이다

---

## 4. 파편 — 판정을 우리 세계로, 계산을 워커로

파편이 프레임의 몸통이었다(`Tick.AutoCannonProjectile 74.8%` 중 71.8%가 `Spall.Burst`).
2D에는 `RaycastCommand`가 없어 `Physics2D`는 메인 스레드 족쇄다.

### TraceWorld — 자체 OBB 충돌 세계

판·모듈 콜라이더를 틱당 한 번 OBB 배열로 뜨고 선분 교차를 순수 산술로 푼다.
**콜라이더가 원본이다. 격자가 아니다** — 경사장갑과 `Placement.size`가 전부
콜라이더 기하에 살기 때문.

권위를 넘기기 전에 **대조 모드**를 먼저 만들었다: 파편 레이마다 Physics2D와 우리
답을 비교해 어긋난 것만 로그.

| 런 | 일치 | 불일치 | 정체 |
|---|---|---|---|
| 1차 | 65,034 | 2,136 | 스냅샷이 낡음(Simulate 경계) + 유폭이 죽인 유령 |
| 2차 | 180,692 | 328 | **전부 거리는 같고 콜라이더만 다름** = 겹친 면의 동점 |
| 3차 | 142,624 | 2 | 표면 위 시작점의 그레이징 |

불일치는 전부 **Physics2D가 애초에 임의였던 자리**로 판명됐다. 그래서 규칙으로
승격했다:

- **동점은 판이 탄을 받는다** (모듈은 판 위에 볼트로 붙어 면이 겹친다)
- **낳아준 면은 도로 안 맞는다** (표면 1mm 안에서 시작하면 그 콜라이더는 없는 것)

### wave 구조

`Burst`는 큐에 넣기만 하고 첫 호출자가 펌프를 돌린다:

```
[세대 단위 읽기 전용 계산] → [orderKey 정렬 후 커밋] → 다음 세대
```

같은 wave의 파편은 서로를 못 본다(같은 순간 도착). 계산 단계가 상태를 안 바꾸므로
그대로 `IJobParallelFor + Burst`로 나갔다 — 64발 이상일 때만(작은 wave는 스케줄
비용이 더 크다). 충각·유폭은 `DeferPump` 스코프로 한 wave에 묶는다.

`orderKey`는 **잡을 넣기 전부터** 있었다. 싱글스레드일 때 정렬은 항등이지만,
워커 완료 순서가 결과에 새는 문을 미리 닫아 둔 것이다.

Burst 잡은 `FloatMode.Strict + CompileSynchronously`로 고정했다. 기본값은 근사를
허용하고, 동기가 아니면 **에디터에서 Burst 준비 전 몇 프레임이 관리 IL로 돌아
같은 세션 안에서 답이 갈린다.**

---

## 5. 틱 루프와 잡다한 것들

- `TickManager`: 핫루프의 `IsDestroyed`(리스너 전원 × 2페이즈 × 60틱) 제거 —
  `Destroy`는 지연되고 실제 파괴 시점엔 `OnDisable`이 이미 목록에서 뺀다.
  early/late 리스트 분리로 no-op 리스너(판·문·엔진) 전수 2회 완주도 제거
- `StillAboard`: `GetComponentInParent<Ship>()` → `transform.IsChildOf`
- `NearestHostile`: 틱스탬프 캐시 — 포탑 N문이 같은 답을 N번 전수 스캔하고 있었다
- `Hulk.OnTick`: `GetComponentInChildren<Armor>()` → `AliveCount` 장부
- `MainUIManager`: `FindObjectsByType`를 **물리 스텝마다** → `Ship.All` + 값이 변할 때만 대입
- `DamageLog`: `RemoveAll(람다)`가 매 프레임 클로저 할당 → 역방향 for
- 관통음: 서브셀당 → 판당 프레임당 1회 (그라인딩이 이 함수를 틱당 수천 번 부른다)
- 배 그림 PNG: 파일명 키 정적 공유 (디코드 1회, 픽셀 사본 ~37MB 1벌)

---

## 6. 할당 사냥 — "프레임마다 새로 만드는 것"

프로파일이 `Ship.Split` 26%를 가리켰는데 그 안에 `GC.Collect 3ms`가 있었다.
파단 한 번에 **726건 254KB.**

| 자리 | 무엇이 태어났나 | 바꾼 것 |
|---|---|---|
| `Ship.Split` | `GetComponentsInChildren<T>()` 배열 4개 | List 오버로드 |
| `SplitRear` | 조각마다 `HashSet`, bbox 배열 셋 | 평면 owner 표 + 정적 스크래치 |
| `TrySplitIfBroken` | `new HashSet(_rear.Keys)` 1,200칸 복사 | `_rear.Keys` 직접 전달 |
| `WithHealth` | 조각마다 `Dictionary` | 삭제 — 장부 한 번 순회로 채운다 |
| `BuildStructure` / `ToDesignCells` / `byCell` | 조각 List·사본·사전 | 정적 재사용 |
| `Radiate` / `Conduct` 재진입 | `Collider2D[1152]`(9KB)를 폭발마다 | **깊이별 버퍼 풀** |
| `Punch` | `HashSet<Armor>` 중복 제거 | `PunchStamp` 도장 |
| `ImGui.Get(id, () => new X(...))` | 팩토리 람다가 **캐시 히트에도** 클로저+델리게이트 | `Existing`(조회) + `Adopt`(미스일 때만) |
| `Dialogue` 위젯 id | `line.id + "_plate"` 8개를 **매 프레임** | 생성자에서 한 번 |
| 패턴 행 id | `$"comm_pattern_{row}"`를 행 수만큼 매 프레임 | 행 번호 캐시 |
| 판 붕괴 / `MakeDebris` | `GetComponentsInChildren` 배열 (판 사망마다) | List (모듈 쪽은 깊이별) |

**괜찮다고 판정한 것들도 적어둔다** — 이게 다음 사람의 시간을 아낀다:

- `Update`/`OnTick` 안의 `new`는 전부 `Vector2`/`Rect`/`DeterministicRng` — **구조체라 힙이 아니다**
- LINQ는 이 리포에 한 줄도 없다
- `_requests` `Queue<Request>`는 구조체 큐라 워밍 뒤 무할당이고, 파면을 넘나드는 수명이 필요하다

---

## 7. 틀렸다가 되돌린 것 — 이 문서에서 제일 값어치 있는 절

### ① 74.8%를 분해 안 하고 탄 배치를 제안했다

`Tick.AutoCannonProjectile 74.8%`를 보고 "탄을 배치로 병렬화하자"고 했다. 펼쳐
보니 그 74.8% 중 71.8%가 **탄이 낳은 파편**이었고, 탄 자신은 self 0.8% + 레이
0.1% — 363발에 1.3ms였다. 완벽히 병렬화해도 130ms 프레임에서 1ms.

> **합계를 보고 결정하지 마라. 펼쳐서 self를 봐라.**

### ② 파편 예산의 안전장치가 예산을 무력화했다

"밀린 파편 > 예산의 4배면 예산 무시"로 catch-up을 걸었는데 **큰 폭발 하나가 그
문턱을 즉시 넘겨서** 정작 눕히려던 그 순간에 예산이 꺼졌다.

빚은 개수가 아니라 **시간**으로 재야 한다 → `SpallMaxLagTicks`(8틱). 폭발이 아무리
커도 8틱에 걸쳐 퍼지고, 지속적인 초과 생산만 따라잡기로 넘어간다.

### ③ 예산이 오히려 렉을 늘렸다

`ComputeParallel`이 **파면마다** 세계 전체(OBB 1,500개 × 56바이트)를 NativeArray로
복사하고 있었다. 예산이 파면 수를 8배로 늘리자 그 고정비도 8배가 됐다.

세계 배열을 영속으로 살려 두고 스냅샷이 바뀔 때만 채우게 고쳤다. 앞서 "TempJob은
싼 범프 할당이니 넘어가자"고 판단했던 바로 그 자리다.

> **고정비는 반복 횟수를 늘리는 순간 병목이 된다.** 배치를 쪼개는 최적화를 넣을
> 때는 "한 배치의 고정비"를 먼저 봐라.

### ④ wave 끝 무조건 무효화 → 재굽기 폭풍

정확성 우선으로 "wave 끝마다 스냅샷 무효화"를 넣었더니 펌프 21회 × 콜라이더
1,500개 재굽기가 프레임 100ms를 만들었다. 죽음 깔때기 신고로 바꿨더니 이번엔
그라인딩(매 틱 판 사망)에서 매 틱 재굽기.

최종형: **재굽기는 틱당 최대 2회**(전반 + Simulate 후). 유령은 채택할 때 생존
검사로 거른다. 같은 자리를 세 번 뒤집었고 그 역사를 주석에 박아뒀다.

### ⑤ Sprite UV가 0~1이라고 가정했다

후면 셰이더에 쿼드 폭을 그대로 넘겼는데 `Sprite.Create`의 uv는 **0~1이 아니라
`rect / 텍스처크기`**다. destroyer에서 uv.y가 0~0.33밖에 안 돌아 **설계도 아래
3분의 1이 화면 전체로 늘어났다.** 셀프테스트로 못 박았다.

### ⑥ 브랜치를 확인 안 하고 몇 시간을 일했다

작업 브랜치가 낡은 main에서 갈라져 있었고, 오너의 실제 작업(다각형 판, Vent 칸,
비대칭 화면, 후면 16ppu 렌더러)을 모르는 채로 최적화했다. 프로파일 수치도 그동안
낡은 코드를 재고 있었을 수 있다. 포팅에 시간을 다시 썼다.

> **시작 전에: 본체가 어느 브랜치인가, 워크트리 베이스가 최신인가.**

---

## 8. 이 리포에 반복해서 나온 모양 셋

1. **깊이 카운터 + 깊이별 버퍼** — `_conducting` / `_radiating` / `_collapseDepth`.
   피해 연쇄가 동기로 재진입하는 코드베이스라, 정적 버퍼를 그냥 쓰면 안쪽 폭발이
   바깥 폭발의 상태를 갈아엎는다. 세 번 나왔다.
2. **도장(stamp)** — 집합 대신 정수. 지울 일이 없다. 단 **주인이 다르면 필드도 달라야 한다.**
3. **정적 재사용 + "재진입 없음" 논거** — 파단·BFS는 OnTick 앞에서만 돌고 그 안에서
   피해가 안 나간다. 이 한 문장이 정적 버퍼를 허락하는 근거이고, 주석에 매번 적었다.

---

## 9. 남은 목록 (순위)

1. **`ThingDef.Spawn`의 프로토타입** — 스폰마다 JSON 원문을 컴포넌트 수만큼 다시
   파싱한다. 포탄 한 발 = 파싱 2회 + `AddComponent` 여러 번, 함선 소환 = 600회.
   답은 비활성 프로토타입 + `Instantiate`(네이티브 복사)
2. **`ShipDef.Load` 3중 호출** — 스폰당 같은 파일을 3번 읽고 파싱하고 리플렉션 검증
3. **`Atmosphere` 방×벽 전수** — 피해 유무와 무관하게 틱당 ~450회 검사. 더티 캐시
4. **`SweepNearColliders`의 `_colCache` 사전 조회** — 할당이 아니라 조회.
   `TraceWorld._sources`가 같은 캐시를 이미 든다
5. **`BuildStructure` 반환 조각 List 풀링** — 호출자가 소유권을 들어 수명 감사 필요

**게임플레이 결정 (오너 몫, 미적용)**

- 파편 수 자체: `SpallMaxCount 24` / `CollapseFragmentCount 6` — 판 한 장 붕괴가 ~240발
- 잔해 수명: 1판짜리 잔해에 짧은 `lifeTick`
- 레이어 매트릭스: 잔해×잔해 솔버 충돌 끄기
- `Physics2D.useMultithreading`은 켜져 있는데 `useConsistencySorting`이 꺼져 있다 —
  접촉 순서가 스레드 스케줄에 달려 **리플레이 결정론과 충돌**할 수 있다

---

## 10. 빌드 전 체크

- **Always Included Shaders에 `SUPERRADIANCE/PlateSkin`과 `SUPERRADIANCE/RearSkin`**
  둘 다 추가. 런타임 `Shader.Find`라 참조가 없어 빌드에서 스트리핑된다
- `Tools > Ship > Run Ship Grid Tests` — 격자·파단·후면·링 검사·실루엣·쿼드 UV
- `Tools > Ballistics > Run Penetration Tests` — 관통·잡 슬롯 순서·예산 배수·충격량
