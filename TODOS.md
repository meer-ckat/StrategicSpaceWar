# TODOS

미룬 것들. 이유와 배경을 같이 적는다 — 맥락 없는 한 줄짜리 TODO는 없느니만 못하다.

## 잔해도 쪼개져야 한다

**무엇** — `Hulk`가 한 번 떨어져 나온 뒤로는 다시 갈라지지 않는다. 3장짜리 잔해의
가운데 판을 쏴서 없애도, 남은 두 판이 서로 안 닿는데 하나의 리지드바디로 붙어 다닌다.

**왜 지금 안 하나** — `Ship.SplitIfBroken`은 칸 좌표 격자 위에서 8방향 BFS를 돈다.
`Breakaway`가 판을 새 GameObject로 재부모화하면서 `localPosition`이 바뀌어 그 격자가
사라진다. 잔해에 같은 BFS를 돌리려면 격자를 들고 다니거나, 다른 방법이 필요하다.

**어떻게 할 것인가** — 격자를 들고 다니지 말고 **기하로 잇는다.** 재부모화 뒤에도 판들은
여전히 같은 1 m 격자 위에 있으므로, 두 판의 중심 거리가 `1.5 * ShipGrid.CellSize` 이내면
이웃으로 친다(대각 1.41까지 포함, 8방향과 같은 결과). 이러면 잔해가 격자 없이도 쪼개지고,
같은 함수를 나중에 함선 쪽에도 쓸 수 있다.

**의존** — 없음. 판 하나짜리 잔해가 흔해지면 그때가 할 때다.

## 결정론이 지금 깨져 있다

**무엇** — `Ballistics.Hash(GetInstanceID(), tick, i)`. Unity의 인스턴스 ID는 실행마다 달라서
같은 입력이 같은 결과를 주지 않는다. 파편 방향도, 판 붕괴도, 잔해 초기 속도도 매번 다르다.
`CLAUDE.md`의 "결정론" 불변식이 문서에만 있고 코드에는 없는 상태다.

**왜 지금 안 하나** — 안정적인 ID를 만들 자리가 이제 막 생겼다. `ShipDef.placements`의 인덱스가
그 배 안에서 영원히 안 변하는 번호다. 그걸 `Thing`까지 실어 내려야 하는데, `ShipBuilder.Spawn`이
심을 때 넣어주면 된다.

**어떻게** — `Thing.stableId`를 추가하고 `Spawn`이 배치 인덱스를 넣는다. 해시는 그걸 쓴다.
씬에서 손으로 지은 배(= export 원본)는 자식 순서를 쓰면 된다. 리플레이나 시드 재현이 필요해지는
순간이 할 때다.

## 서브셀이 아직 6×6 고정

**무엇** — `Ballistics.SubGrid`가 컴파일 상수 6이다. 콜라이더가 1×1이든 1×1.41이든 6×6으로
나눈다. 경사판은 그래서 서브셀이 정사각형이 아니고, 2×2 판은 1×1 판과 같은 36칸으로
나누므로 손상 해상도가 판 크기에 반비례해 거칠어진다.

**지금 망가지는 것은 없다** — 재료 밀도는 `hpPerSquareMetre`로 이미 균일해졌다. 남은 것은
해상도 문제뿐이라 급하지 않다.

**어떻게** — 합의된 설계는 이렇다:
- `SubCellSize = 1/16 m`를 Tuning에 두고 개수는 콜라이더 크기에서 파생. 상한 32×32
- `MaxLanes` 5 → 8. s=1/16이면 400 mm 탄이 6.4레인을 원하는데 5에서 잘려 판 가장자리가 성해진다
- 고아 쓸기(`Armor.KillOrphans`)를 지연시킨다. 지금은 서브셀이 죽을 때마다 판 전체를 다시
  훑어서 O(n²)다. 6×6에서 1,296이던 것이 32×32에서 100만이 된다
- 위 둘은 6×6에서는 **아무 일도 안 한다.** 해상도를 올리는 변경과 같이 들어가야 의미가 생긴다

**진짜 비용은 x-ray UI다** — `HitXrayUI.cs`(591줄)가 6×6 GUI 격자를 고정으로 깔고 있다.
판마다 크기가 다르면 셀 배열을 판 선택할 때마다 다시 만들어야 한다. 그게 싫으면
x-ray만 항상 고정 크기로 리샘플해 보여주는 길도 있다(표시 정밀도만 손해).

**의존** — 없음.

## 판 모양 마스크

**무엇** — 사다리꼴·삼각형 판. 서브셀이 콜라이더 바운딩 박스를 덮으므로, 모양 밖 칸은
`bool[] solid` 마스크로 미리 죽여야 한다. 모양 밖은 RHA 0을 낸다.

**왜 지금 안 하나** — 동적 서브그리드 뒤에 하면 배열 하나 추가로 끝난다. 먼저 하면 두 번 짠다.

**불변식이 바뀐다** — "신선한 판은 nominal RHA를 읽는다"가 "선을 따라간 **실물 비율만큼의**
nominal RHA를 읽는다"가 된다. 재정규화하면 안 된다 - 사다리꼴 얇은 쪽이 두꺼운 쪽과 똑같이
막게 된다.

## 유폭 한 번에 레이캐스트 수천 개

**무엇** — 탄약고가 터지면 판 40장이 같은 틱에 무너진다. 판 하나가 무너질 때 죽는 서브셀마다
`Armor.Collapse`가 파편 6개를 쏘고, `PlateCollapseFraction`이 0.5라 판당 약 18칸이 죽는다.
18 x 6 x 40 = **약 4,300 레이캐스트가 한 틱에** 몰린다.

**왜 지금 안 하나** — 아직 프레임이 튀는 걸 못 봤다. 재보지도 않았다. 안 아픈 걸 고치는 것은
투기고, 유폭은 판당 한 번 있는 사건이라 상시 비용이 아니다.

**언제 할 때인가** — 유폭 순간에 눈에 띄는 히치가 보이면. 그때 고치는 법은 둘이다:
- 판이 통째로 무너질 때는 서브셀마다 쏘지 말고 `CollapseRemains`가 한 번에 크게 쏜다
- 또는 붕괴 파편에 틱당 상한을 둔다 (`SpallMaxCount`가 버스트당 상한이지 틱당은 아니다)

**같이 봐야 할 것** — `SpallTrails.Capacity`를 4096으로 올렸는데, 그것도 이 숫자에서 나왔다.
파편 수를 줄이면 용량도 되돌릴 수 있다.

## Unity 6에서 obsolete가 된 물리 질의 API

**무엇** — `Physics2D.RaycastNonAlloc`과 `Physics2D.OverlapCircleNonAlloc`이 Unity 6에서
`[Obsolete]`다. 대체는 `ContactFilter2D`를 받는 오버로드이고, 그쪽은 `List<T>`도 받아서
버퍼 크기를 넘긴 결과가 잘리지 않는다.

쓰는 자리 넷:
- `Projectile.Surfaces` — 본체 레이캐스트
- `Projectile.Damage` — 모듈 직격
- `SpallResolver` — 파편
- `RamImpact.Radiate` — 유폭이 빈 공간을 건너갈 때

**왜 지금 안 하나** — 경고지 에러가 아니다. 그리고 넷 중 하나만 바꾸면 같은 일을 하는 코드가
두 모양이 되어 다음 사람이 어느 쪽을 따라야 할지 모른다. 옮기려면 넷을 한 번에.

**같이 볼 것** — 넷 다 레이어 마스크를 인자로 받는데 `Radiate`만 마스크 없이 컴포넌트로
거른다. `ContactFilter2D`로 옮기면 그것도 마스크 쪽으로 통일된다.

## 죽은 함장이 아무 흔적도 안 남긴다

**무엇** — `battle-lost.json`이 `Atrian Navy loss record updated.`라고 말하는데 **기록되는
데가 없다.** 대사가 거짓말을 하고 있다. `Battle.End(false)`가 `RunState.Clear()`를 부르고,
그것이 배 파일·진행도 파일을 지운 뒤 `RunLog.Clear()`까지 한다. 런을 넘어 사는 저장이
하나도 없다.

STORY.md의 핵심 테마가 연속성이다 — CAPTAIN A가 죽으면 CAPTAIN B가 같은 좌표로 향하고,
"플레이어 캐릭터 한 명이 주인공이 아니라 아트리아 해군의 작전 자체가 주인공"이다. 그게
지금 게임 안에서 안 보인다.

**왜 지금 안 하나** — ACT I 수직 슬라이스에서 뺐다. 함장 이름을 붙이는 것(`Play(script, arg)`의
`{0}` 치환)은 이미 되는 것이라 ACT I에 들어가지만, 이름을 **누적해서 보여주는** 것은 새
영속 저장이 필요하고 그걸 보여줄 화면도 없다. 화면 없이 파일만 쌓는 것은 아무것도 아니다.

**어떻게** — `persistentDataPath/losses.json`에 append. 런이 끝날 때 함장 이름·도달 구역·
사인 한 줄. 다음 런 프롤로그가 마지막 항목을 읽어 언급한다. `RunState.Clear`가 이 파일은
안 건드려야 한다 — 지금 "런 파일의 주인이 하나"라는 규칙이 있는데 이건 런보다 오래 사는
것이라 그 규칙 **밖**에 둬야 한다.

**의존** — 보여줄 자리. ACT II의 항로 화면이나 시작 화면이 생기면 그때가 할 때다.
