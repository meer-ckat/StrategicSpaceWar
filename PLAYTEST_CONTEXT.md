# SUPERRADIANCE 플레이테스트 컨텍스트

## 세션 정보

- 날짜: 2026-09-06
- 대상 빌드: `Builds/StrategicSpaceWar.exe`
- 목적: 실제 게임 진행 중 보이는 화면, 조작 감각, 정보 전달, 막히는 지점을 기록한 뒤 유저 관점으로 검증
- 상태: 재시도에서 Windows 전용 `node_repl` + `@oai/sky` 연결 성공. Unity 창 식별 완료. 대상과 캡처 화면이 달라 실제 플레이는 아직 시작하지 못함.

## 관찰과 추정을 분리하는 규칙

- `실제 관찰`: 게임 화면과 입력 결과를 직접 확인한 내용
- `코드 근거`: UI/게임 코드에서 확인한 내용
- `추정`: 실제 플레이 확인 전 가설. 플레이 재개 시 검증 필요

## 실제 관찰

- 아직 없음. 네이티브 창 연결 문제로 플레이를 시작하지 못함.

## 코드 근거 기반 플레이 경로 가설

1. 함선 선택 화면에서 함선 도식, 질량, 승무원, 종단속도, 추진제, 무장을 확인한다.
2. 조작 힌트가 이동 → 마우스 조준 → 사격 → 부스터 → 기압도 순서로 나타난다.
3. 전투 중 HUD가 속도, 접근속도, ΔV, 기압, 무장 상태, 예측 조준점을 보여준다.
4. 접촉은 먼 거리에서 미확인으로 표시되고 가까워지면 적/아군으로 식별된다.
5. 명중 결과는 관통, 도탄, 비관통, 후면 장갑 유지 등 짧은 문장으로 표시된다.
6. 적은 주포·기관·전력 상실을 선체 손상률과 함께 표시한다.
7. 전투 종료 뒤 항로 화면에서 적 수, 표적 시설, 정비 가능 여부, 회수 자재를 보고 다음 구역을 고른다.
8. 정비 화면에서는 노획 자재로 가장 많이 손상된 판부터 복구한다. 없어진 판과 모듈은 되살리지 않는다.

## 재개 시 우선 확인할 장면

- 첫 함선 선택: 통계가 실제 선택 판단에 충분한가?
- 첫 교전: 적 식별, 조준점, 사격 결과가 한눈에 이어지는가?
- 첫 손상: 기능 상실이 선체 비율보다 잘 읽히는가?
- 첫 승리: 파괴 결과가 노획량·정비 선택으로 자연스럽게 이어지는가?
- 첫 항로 선택: 적 위험과 정비 보상이 손상 상태에 맞는 결정을 만들며, 선택 결과가 명확한가?

## 검증 메모 템플릿

각 장면마다 다음을 기록한다.

- 장면/목표:
- 플레이어가 예상한 것:
- 화면이 실제로 알려준 것:
- 조작 결과:
- 혼란·지연·오독:
- 재미가 생긴 순간:
- 실패해도 다시 시도하고 싶은 이유:
- 개선 우선순위:

## 현재 차단 이슈

- 첫 시도의 `cua_repl` 경로는 브라우저만 제공했으나, 재시도에서 별도 `mcp__node_repl__js` 도구를 발견해 Windows 연결 성공.
- Unity 6000.3.8f1의 StrategicSpaceWar / SampleScene 창이 목록에 있음.
- Unity를 지정한 첫 캡처가 다른 앱 화면을 반환함. 해당 화면에는 입력하지 않았고 내용도 기록하지 않음.
- 자동 승인 검토가 후속 캡처를 게임 외 개인 문서 노출 위험으로 차단함.
- 창 목록으로 Unity 대상을 재확인한 뒤 활성화를 시도했으나 `user input was detected in this window; call get_window_state before continuing` 오류 발생.
- 재개 조건: 사용자가 Unity 게임 창을 전면에 둔 뒤 조작을 잠시 멈춘 상태에서 대상 화면 다시 확인.
