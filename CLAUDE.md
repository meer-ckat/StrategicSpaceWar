# SUPERRADIANCE (repo: StrategicSpaceWar)

2D 사이드뷰 함선 시뮬레이션 로그라이크. Unity, URP, 2D.
우주 로그라이크 × War Thunder × Teardown — 쏜 곳이 실제로 부서지고, 그 결과를 안고 끝까지 간다.

## 핵심 원칙

특수한 결과를 스크립트로 가짜 구현하지 않는다. 기본 규칙을 만들고, 상황은 거기서 자연발생시킨다.
충각·선체 절단·엔진부만 분리·주포만 살아남은 반파 함선은 전부 별도 이벤트가 없다.

## 작업 방식 — 설명이 먼저다

이 리포의 목표는 돌아가는 코드가 아니라, **오너가 이해하고 있는** 돌아가는 코드다.
이해 못 하는 시뮬레이션은 디버깅이 안 되고, 이 프로젝트는 디버깅이 전부다.

- **코드 전에 핵심 로직을 설명한다.** 무엇을 어디에 왜 넣는지, 새로 생기는 규칙이 무엇인지,
  기존 불변식 중 무엇을 건드리는지. 코드 없이, 짧게.
- **오너가 이해했다고 하기 전에는 파일을 건드리지 않는다.** "일단 짜고 설명" 금지.
- **이해가 막히면 그때 할 일은 코드가 아니라 설명이다.** 다른 각도로 다시, 숫자 예시로,
  최소 예제로. 이해될 때까지. 못 알아들었는데 코드가 나오면 그 코드는 실패한 것이다.
- **"이해했지?"로 확인하지 않는다.** 결과를 예측시켜라 — "이 값을 두 배로 하면 어떻게 되나",
  "이 조건이 false면 어디로 가나". 답이 틀리면 아직 설명이 끝난 게 아니다.
- **설명이 길어지면 설계가 복잡한 것이다.** 코드를 더 쓰지 말고 설계를 줄여라.
- **버그를 잡을 때마다 두 가지를 남긴다** — self-test 케이스 하나, 그리고 코드만 봐서는
  안 보이는 것이었다면 아래 불변식에 한 줄.

예외 — 설명 없이 바로 해도 되는 것: 오타, 이름 변경, 포맷, 이미 설명하고 승인된 것의
이어짓기, 오너가 "설명 됐고 그냥 해라"라고 말한 경우.

## 시뮬레이션 불변식

디버깅으로 알아낸 것들이라 코드만 봐서는 안 보인다. 건드리기 전에 읽을 것.

- **틱 순서** — 힘 → `Physics2D.SyncTransforms` + `Simulate` (틱당 1회, `simulationMode = Script`) → `ITickLate`(탄). 탄은 정착된 스냅샷 위에서 판정받는다.
- **탄착으로 미는 것은 결과 배율이 아니라 운동량 보존이다** — 맞은 몸이 받는 충격량은 `탄 질량 × (입사 월드속도 - 이탈 월드속도)`이고, 관통/비관통 0.5x·1x 같은 분기는 없다. 명중점에 `AddForceAtPosition(..., Impulse)`로 넣어 병진과 회전을 같은 규칙에서 낳는다. 판은 피해 적용 중 잔해로 재부모화될 수 있으므로 Rigidbody는 피해 전에 붙잡는다.
- **TraceWorld의 콜라이더 기하는 생성 뒤 고정이다** — `BoxCollider2D.size/offset`, 레이어, Armor 여부는 Source 등록 때 한 번 캐시하고 이동·회전·좌우 반전만 매 틱 Transform에서 월드 OBB로 갱신한다. 런타임에 이 네 값을 바꾸는 기능을 만들면 반드시 Source 재등록 경로도 같이 만든다. 새 `HullStructure`는 다음 스냅샷에서 자동 등록된다.
- **저항과 피해는 같은 선을 읽는다** — 유효 RHA도, 피해 분배도 탄이 판을 가로지르는 채널 전체. 진입 칸만 저항하면 6×6 격자의 5/6이 장식이 된다.
- **실물 비율은 값에 곱하고 가중치에 접지 않는다** — 서브셀이 판의 폴리곤에 얼마나 잠겼는지(`_solid`)는 `EffectiveRhaAt`의 **결과**에 곱한다. `weights` 쪽으로 옮기면 `Ballistics.SubCellPath`가 합을 1로 재정규화하면서 빈 부분이 분모에서 사라져 **삼각형의 뾰족한 끝이 밑동만큼 막는다.** "빈 데는 안 지나가니까 가중치 0"이 직관적이라 반드시 한 번 헷갈리는 자리다. 그리고 가중치의 합이 1인 것이 "멀쩡한 판은 명목 RHA 그대로"를 보장하는 근거이므로, 그 합을 건드리는 것은 폴리곤이 없는 판까지 같이 바꾸는 것이다. 넓이 클리핑이 정확한 것은 **총량(HP 예산)뿐**이다 - 경로는 서브셀 하나가 반만 잠기면 꽉 찬 쪽으로 지나간 탄도 0.5를 읽는다. 계단이 그라데이션이 된 것이지 오차가 없어진 것이 아니다.
- **모서리 폴백** — 접촉면 중 탄을 마주보는 것이 하나도 없으면(`MinFacing` 미달) 정면 입사로 판정한다. 이음매의 접선 normal을 곧이곧대로 읽으면 정면 사격이 공중에서 도탄하고, 버리면 벽을 통과한다.
- **진입 서브셀은 채널이 실제로 지난 첫 칸이다** — 히트 지점은 격자선에 정확히 걸리는 일이 상시라(모서리 명중, 격자선 명중), `floor()`만 쓰면 2.2% 확률로 탄이 *떠나는* 칸을 고른다. 채널이 DDA(`Ballistics.March`)라 그 칸의 **통과 길이가 0**이고, 그래서 무게도 못 받고 진입 칸도 못 된다 - 특수 처리가 아니라 길이가 0이라는 사실 하나로 걸러진다. **`EntrySubIndex`와 `SubCellPath`가 같은 순회를 부르는 것이 규칙이다** - 예전에는 진입 칸을 "첫 샘플 위치"로 따로 구했고, 그래서 샘플 간격을 바꿀 때마다 정의가 같이 움직였다. 둘이 갈라지면 증상은 "진입 칸이 채널 밖에 있다"뿐이라 눈에 안 띈다.
- **두 BFS는 정반대 그래프** — 방은 빈 칸을 4방향으로, 선체는 실물을 8방향으로 잇는다. 계단식 판을 4방향으로 보면 멀쩡한 배가 두 동강 난다.
- **링 검사는 선체와 같은 8방향이고, 틀리는 방향이 비대칭이다** — `ShipGrid.RemovalMightSplit`은 죽는 칸의 살아 있는 8이웃이 3×3 링 안에서 한 덩어리면 파단 BFS를 생략한다. false("절대 못 가른다")가 틀리면 갈라져야 할 배가 안 갈라지는 시뮬 버그, true("몰라서 BFS에 묻는다")가 틀리면 헛BFS 한 번이다. 4방향으로 세면 L자 모서리의 대각 연결을 놓쳐 매번 헛BFS고, ㄷ자처럼 링 밖으로 돌아 이어지는 모양은 링이 알 수 없어 true가 정답이다.
- **좌우 반전은 월드에만 있다** — 반대쪽에서 오는 함선은 `localScale.x = -1`이고, `localPosition`은 부모 scale과 무관하므로 `ShipBuilder.Stamp`가 보는 값은 정방향 배와 **글자 그대로 같다.** 격자·방·구조 BFS는 전부 로컬 위상이라 손댈 것이 없다. Stamp에 scale을 곱하면 1차 패스(원점)와 2차 패스(샘플)가 다른 공간을 쓰게 되고, 칸 번호에 -1을 곱하는 순간(반사는 `-x`가 아니라 `width-1-x`다) 인덱스가 음수로 나가 배열 밖으로 나간다. 반전을 실제로 처리할 곳은 격자가 월드로 나가는 세 자리뿐이다: `RoomView`와 `BackPlateView`의 오버레이 scale(렌더러가 함선의 자식이 아니라서 안 물려받으며, 런타임 반전도 따라가도록 매 프레임 읽는다), `HullStructure.Breakaway`의 잔해 scale(scale이 다르면 `worldPositionStays`가 자식의 `localPosition.x` 부호를 뒤집어 칸 좌표를 통째로 망친다).
- **row는 아래로 증가한다** — `ShipGrid.Map.ToLocal`이 `origin.y - row * CellSize`다. 그래서 칸 좌표계와 배 좌표계는 **x축 대칭**이고, 둘 사이를 오갈 때 각도와 y 성분이 **같이** 부호를 바꾼다. `Placement.rot`도 `Placement.offset`도 배 좌표계로 적어야 한다 - 칸 번호로 도형을 계산한 뒤 그대로 적으면 경사면이 거울상이 된다. 증상이 고약한 것은 **배가 여전히 지어지고 방도 멀쩡하다**는 것이다: 격자 위상은 대칭이라 아무 검사도 안 걸리고, 틀린 것은 눈에 보이는 기울기뿐이다. 손으로 지은 45° 판이 기준점이다 — 오른쪽으로 갈수록 **올라가는** 선이 `rot`이 음수다.
- **격자와 콜라이더는 다른 층이다** — 방·선체·파단은 1m 정수 격자와 판의 *위치*만 읽는다. 콜라이더 크기(2×2까지)·회전·모양은 탄도와 그림만 쓴다. 경사장갑을 위해 콜라이더를 키운 것이 방 구획을 바꾸면 안 된다. 대가: 2×2 판이 덮은 이웃 칸이 격자상 비어 있으면 방은 그리로 이어진다. 버그가 아니라 이 결정이다.
- **실물이 있는 것과 공기를 막는 것은 다른 질문이다** — `ShipGrid.Solid`가 예전에는 둘 다에 답했다. `Cell.Vent`(def의 `sealsRoom: false`)가 그 둘을 가른다: 안테나·마스트·노즐은 실물이라 격자에 있고 `Armor.Neighbours`에도 있고 8방향 구조 BFS에도 들어와 **쏘면 끊기고 잔해가 되지만**, 방은 안 만든다. 그래서 `Solid`에는 Vent가 **들어가고**(구조·`_alive`·판 소실), `MarkExterior`의 flood **두 자리**만 `Airtight`(= `Solid && !Vent`)를 본다. Vent를 `Solid`에서 빼는 쪽이 훨씬 자연스러워 보이는데 그러면 둘이 한꺼번에 죽는다 - 안테나가 구조 BFS에서 빠져 안 끊기고, `MarkExterior`의 **마지막 칠하기 루프**가 `Solid`로 걸러내는 탓에 Vent 칸이 통째로 Exterior로 덮어써져 값이 태어나자마자 사라진다. 증상은 에러가 아니라 "아무 일도 안 일어남"이다. 셋째 술어 `BackPlate`에서는 Vent가 **빠진다** - 우주에 튀어나온 자리라 뒷벽이 없다. 넣어두면 안테나 뒤에 보이지 않는 장갑이 생기고 증상은 "이상하게 단단하다"뿐이다. 그리고 격자를 찍는 길이 둘이라(`Stamp`는 살아 있는 컴포넌트, `StampFromDef`는 오브젝트 없이 def) `sealsRoom`이 `Armor`와 `ThingDef` 양쪽에 있어야 한다. 크기로 자동 판정하지 않는 이유는 임계값을 두면 판을 조금 키웠다가 배 한가운데 방이 갑자기 생기기 때문이다 - 어떤 판이 밀폐하는지는 설계자가 정한다.
- **후면은 "우주에 닿지 않은 Vent"에도 깔린다** — `ShipGrid.BackPlate`가 Vent를 빼는 전제는 "Vent = 우주에 튀어나온 안테나"였는데, `Strut`(sealsRoom:false)이 선체 안쪽 바닥으로 쓰이면서 깨졌다. 안 얹으면 Strut 칸 뒤로 탄이 후면 저항 없이 나가고 외피 그림에 그 칸만 구멍이 뚫린다 - destroyer 모듈 밑 판 87장 중 46장이 Strut이라 증상은 "모듈 자리마다 외피가 뚫려 있다"로 나온다. 판정은 `HullStructure.RearWorthy` 하나다(8방향에 우주가 닿으면 제외 - 안테나·열린 노즐). SeedRear·LostRear·RearBreached 셋이 전부 이 술어를 써야 한다 - 갈라지면 저장·복원에서 Strut 후면만 안 돌아온다.
- **한 장짜리 판 이탈도 후면 소유권 이동이다** — 전체 파단은 `SplitRear`가 후면을 나누지만 `Armor` 붕괴의 `Shed`는 그 경로를 타지 않는다. 같은 설계도 칸의 `RearCell` 하나를 명시적으로 같이 넘겨야 한다. 소형 시각 잔해면 함께 사라지고 정식 `Hulk`면 같이 분리된다. 성공 전에 본체에서 빼면 잔해 생성 실패 때 후면만 사라지므로, 이동 성공 뒤에만 커밋한다.
- **한 장짜리 시각 잔해는 자식 원점이 곧 회전축이다** — 잔해 루트를 원래 함선 원점에 둔 채 `centerOfMass`만 판 자리로 바꾸면 Transform 원점이 먼 곳에 남아 다시 공전할 수 있다. 판의 월드 위치를 보존하면서 잔해 루트를 그 자리로 옮기고, 판 `localPosition`과 Rigidbody2D `centerOfMass`를 둘 다 0으로 접는다. 구조 격자를 계속 읽는 정식 `Hulk`에는 이 재중심화를 하면 안 된다.
- **후면 발자국은 배치 칸에 갇히지 않는다** — `RearCell.footprint`는 판의 size·offset·rotation·shape를 이미 배 좌표로 합친 최종 실루엣이다. BackPlate 마스크는 그 폴리곤의 실제 극값까지 전역 설계 좌표에 굽고 쿼드도 같은 만큼 넓힌다. 1×1 anchor 칸 안에서만 굽거나 rear bbox까지만 쿼드를 만들면, 경사판이 칸 밖으로 나간 부분에는 덮을 픽셀 자체가 없어 아래 Armor가 쐐기처럼 비친다. 후면 HP는 여전히 칸 단위다 — 넓어지는 것은 그림뿐이다.
- **후면의 모양은 격자가 아니라 판의 기하에서 나온다** — 후면의 *존재*는 칸 단위지만(`RearWorthy`, 1m 정수 격자) *모양*은 판 폴리곤의 합집합을 16배 해상도로 굽고 바깥에서 flood해서 얻는다. `MarkExterior`와 같은 규칙("둘러싸인 안쪽이 실내다")을 서브셀에서 한 번 더 도는 것이고, 그래서 판이 하나도 없는 방 한가운데도 후면이다 - flood가 못 닿는 주머니라서. 칸 단위로 자르면 두 가지가 같이 틀린다: 경사 지붕 칸은 우주에 닿은 경계 칸이라 폴리곤으로 깎이는데 **그 칸의 방을 향한 안쪽 절반까지 잘려나가** 함교 천장에 구멍이 뚫리고, 반대로 큰 경사판이 자기 칸 밖을 덮으면 그 칸은 격자상 `Exterior`라 후면이 아예 없다. 유일한 진짜 위험은 **이음매 누수**다 - 인접한 두 판의 폴리곤이 16px 격자에서 1픽셀 벌어지면 flood가 실내로 새서 그 주머니의 후면이 통째로 사라진다. 래스터가 2x2 보수 샘플링(하나라도 실물이면 덮는다)인 것과, 판 없는 실내 칸이 칸 전체로 채워져 방벽이 되는 것이 그 보험이다.
- **밀폐 주머니가 실내다** — 텍스트 맵이 죽으면서 ' '가 주던 우주/실내 구분이 사라졌다. `MarkExterior`가 비실물 테두리 칸에서 4방향으로 번져서 닿으면 우주, 안 닿으면 실내로 정한다. `Cell.Unset`이 0인 이유가 이것이다 — 기본값이 Exterior면 "아직 안 정해진 칸"과 "우주"가 구분되지 않아 flood가 자기 결과를 다시 읽는다.
- **판만 격자에 도장을 찍고, 판은 선체 직속 자식이어야 한다** — `localPosition`은 부모 기준이라 한 겹만 내려가도 칸 좌표가 통째로 틀린다. 모듈(포탑·엔진)은 자기가 붙은 판의 자식으로 정확히 한 겹 아래 들어간다. 판이 죽으면 같이 죽고 잔해로 떨어지면 같이 날아가는 것이 전부 이 부모 자식 관계 하나에서 나온다 — 별도 코드가 없다.

  뒤집으면: **판이 아닌 것은 선체 직속 자식이 되면 안 된다.** 격자를 읽는 자리는 전부 `ShipBuilder.IsPlate`를 통과해야 한다 - 안 그러면 방 오버레이 같은 그림 하나가 칸을 차지해서 진짜 판을 조각에서 밀어내고, 그 그림이 대신 잔해에 실려 날아간다.
- **안 쏘는 것과 안 겨누는 것은 다른 일이다** — 아군 사선 검사는 `Gun.OnTick`의 격발 직전에, 포신이 실제로 향한 선을 포구에서 쏴서 본다. 조준 단계(`TryGetTarget`)에서 검사하면 두 가지가 틀린다: 포신은 아직 선회 중이라 표적 쪽을 안 보고 있고, false를 돌려주면 `_pending`이 0으로 리셋돼 포탑이 표적 추적을 통째로 그만둔다. 막혀서 안 쏜 발은 `_pending`을 소모하지 않아 사선이 열리는 순간 나간다.
- **충각은 매 틱 도니까, 뚫리는 것은 한 방이 세서가 아니라 갉는 쪽이 안 죽어서다** — `RamImpact.Reaction`은 질량비를 **때리는 쪽 기준**으로 보므로 방향마다 뒤집힌다: 배가 잔해를 칠 때는 0.002(사실상 면제, 의도한 것 - 밀면 밀려나야지 부서지면 안 된다)인데 잔해가 배를 칠 때는 0.99다. `Punch`가 매 틱 도니 잔해가 선체에 붙어 있으면 초당 60번 갉고, 10 m/s짜리 느린 접촉도 결국 뚫는다. 진짜 비대칭은 **뜯긴 조각이 본체와 똑같이 단단한 것**이라, `MakeDebris`가 `DebrisHpFraction`만큼 깎아서 잔해가 반작용을 먼저 못 견디게 한다 - 접촉이 스스로 끝난다. 이탈 경로가 `Shed`(한 장)까지 전부 `MakeDebris`로 모여 있어서 한 자리면 된다. 깎는 것은 `ScaleHealth`이고 **값을 놓기만 하지 아무도 안 죽인다** - `ApplyDamageEvenly`로 하면 서브셀이 실제로 죽어서 파단 BFS 한가운데서 붕괴 연쇄가 시작된다.
- **충각의 절단은 별도 코드가 아니다** — `RamImpact.Conduct`의 감쇠가 등방성이 아니라 충격축을 따라 길고 옆으로 짧다(0.80 대 0.30 per m). 그 띠가 반대쪽 외판까지 이어지면 판들이 죽고, 이미 매 틱 도는 `HullStructure.SplitIfBroken`이 두 덩어리를 찾아 가른다. "허리를 끊는" 함수는 없다. BFS와 공식이 하는 일이 다르다는 것이 요점 — **BFS는 어디까지 닿는가**(실물로 이어져 있어야 간다), **공식은 얼마나 먹는가**(위치에서 바로. 경로에 누적하지 않으므로 도달 순서와 무관하다).
- **판의 이웃은 물리로 찾지 않는다** — `Armor.Neighbours`를 `ShipBuilder.Stamp`가 격자에서 한 번 채워 둔다. `OverlapCircle`로 찾으면 세 가지가 틀린다: 호출마다 배열을 할당하고, 콜라이더 반경 때문에 두 칸 건너까지 집어오고, **상대 함선의 판까지 집어온다.** 잔해로 갈라진 뒤에도 참조는 살아 있으므로 `Armor.SameBodyAs`(부모 비교)로 거른다 — 판이 선체 직속 자식이라는 규칙이 여기서 한 번 더 값을 한다.
- **잔해로 간 모듈은 null이 아니다** — 재부모화돼도 `Gun.owner`도 `Ship.shipEngines`도 그대로 살아 있다. 그냥 두면 배가 100m 뒤의 엔진으로 가속하고 날아간 포탑이 계속 쏜다. `Ship.StillAboard`로 매 틱 소속을 다시 확인한다. **`ship*` 목록을 읽는 자리는 전부 이 검사를 통과해야 한다** - `HasPower`(shipCriticals), `AvailableThrust`(shipEngines), `IsCombatEffective`의 포탑 루프(shipGuns), `RepairPlates`(shipArmors). 넷 중 셋만 걸었을 때 증상은 판 세 장짜리 조각이 "우리 배엔 아직 주포가 있다"고 우겨서 **전투가 영영 안 끝나는 것**이었다 - 원인과 한참 떨어진 자리에서 나온다.
- **질량이 주는 자리는 둘이고, 서로 모른다** — `rig.mass`는 판 수에서 나오지만 갱신은 두 사건에서 따로 일어난다: 판이 **부서지면** `Armor.Die` → `ReportPlateLost` 쪽에서, 조각이 **떨어져 나가면** `HullStructure.Breakaway`(`_body.mass`를 조각 몫만큼 뺀다) 쪽에서. 원래 둘째만 있었고, 그래서 제자리에서 부순 판의 질량은 배에 영원히 남았다. **증상이 없는 것이 이 버그의 특징이다** - 배는 멀쩡히 굴러가고 방도 정상이고 아무 검사도 안 걸린다. 틀린 것은 "반파됐는데 안 빨라진다"는 감각뿐이라, 추진제(`Tank.impulse`가 총력적이라 Δv = impulse/mass다)를 넣기 전까지는 아무도 못 본다. 판 수를 읽어 무엇을 파생시키든 이 둘을 같이 봐야 한다.
- **1비트로 압축한 방향은 데드밴드와 만나면 얼어붙는다** — 예전 `engagementSign`이 "접근이 월드 어느 쪽인가"를 부호 하나로 들고, 떨림을 막으려고 25 m 데드밴드를 걸었다. 전투는 `FightDistance`가 120~240 m라 항상 통과해서 20개월을 멀쩡히 굴렀는데, **컷신이 15 m짜리 `moveTo`를 내는 순간** 부호가 마지막 값에 얼어붙어 배가 반대로 밀렸다. 증상이 "프롤로그에서만 이상하다"라 좌표계 전체를 의심하게 되지만 축은 죄가 없다 - **압축이 죄다.** 방향은 벡터로 그대로 들고 다녀라. 압축했다 푸는 왕복이 없으면 그 왕복이 고장 날 자리도 없다. 지금 `Ship.thrustInput`과 `ShipAi.Thrust`가 둘 다 월드 벡터인 이유가 이것이고, **둘이 같은 좌표계인 것 자체가 불변식이다** - 한쪽만 함체 기준으로 바꾸면 AI가 플레이어와 다른 물리를 탄다.
- **사선 검사선과 실제 탄도선은 같아야 한다** — 탄은 포신 방향으로 안 난다. `Projectile.Launch`가 `포신 x 탄속 + 포구 속도`로 쏘기 때문이다(포구 속도는 `v + w x r`라 도는 배의 현측 포탑까지 맞는다). 그래서 `Gun.LineIsClear`가 포신 방향으로만 레이를 쏘면 검사를 통과한 탄이 자기 외판을 긁는다 - 40 m/s에서 두 선이 2.5도 벌어지고, 35 m 반선체 끝에서 1.5 m가 샌다. **`fireArc`가 0.7도(m12)라 조준 정밀도보다 이 어긋남이 크다.** `Gun.MuzzleShot` 하나가 포구·비행 방향·물려받은 속도를 같이 내고 검사와 발사가 그것을 나눠 쓴다. 두 벌로 두면 갈라지고, 증상이 "가끔 내 배에 맞는다"라 원인이 한참 안 보인다. (반동만 예외로 **포신** 방향이다 - 포가 만든 운동량은 포구 속도 몫뿐이라 물려준 속도를 배에서 뺄 이유가 없다.)
- **즉시 모드는 선언에만 있고, 나머지는 리셋해야 존재한다** — `ImGui`는 GUIItem을 캐시에 두고 재사용하므로 Rect·텍스트를 매 프레임 대입해도 `Layer`·`Opacity`·`RenderScale`·`isInteractable`은 리테인드로 샌다. `Materialise`가 이 넷을 매 선언마다 기본값으로 되돌린다. **`Opacity` 기본이 1인 것이 핵심** - 필드 초기값은 0이고 `GUIManager`가 그걸 알파에 곱해서, 안 되돌리면 ImGui 위젯은 전부 "선언은 됐는데 안 보이는 것"으로 태어난다. `Layer`를 안 정하면 전부 동률인데 `GUIManager.BuildDrawRoots`의 `List.Sort`는 **불안정 정렬**이라 동률끼리는 순서가 아예 정의되지 않는다 - 실행마다, 위젯 수가 introsort의 임계(16)를 넘나들 때마다 달라질 수 있다. 그러니 겹치는 것끼리는 반드시 Layer를 명시한다. `isInteractable`은 `GUIItem.Decorative`에서 나온다 - `GUIManager.GetTopMouseLayer`는 마우스 아래 interactable 중 최고 Layer를 찾아 **그보다 낮은 것의 입력을 전부 죽이므로**, 화면을 덮는 배경 라벨 하나가 그 밑의 버튼을 막는다. `GUIGroup`은 장식이 아니다 - 끄면 자식 버튼까지 같이 죽는다.
- **즉시 모드 위젯에 `GUITween`을 걸지 않는다** — 트윈은 핸들러를 `running`/`fading`/`scaling` 정적 사전에 넣고 완주할 때만 뺀다. 선언을 그만두면 위젯이 사라져 완주하지 못하므로 사전만 남아 조용히 자란다. `ImGui.Retire`가 걷을 때 `GUITween.Kill`을 부르지만, 애초에 애니메이션 상태를 선언하는 쪽이 들고 최종값만 매 프레임 대입하면 그 수명 문제가 존재하지 않는다.
- **결정론** — RNG는 `DeterministicRng` + `Ballistics.Hash`뿐. `UnityEngine.Random` 금지.
- **결정론은 def로 지어진 배에만 성립한다** — 시드로 쓰는 것은 `Thing.stableId`이고, 그 값은 `ShipBuilder.Spawn`의 **배치 인덱스**다. 기본값이 -1인 것이 요점이다: 씬에서 손으로 지은 배는 결정론 밖이고, 자식 순서를 ID로 쓰자는 안은 버렸다(에디터에서 드래그 한 번에 바뀌는 값을 "안정 ID"라고 부르면 재현 안 되는 튜닝을 하고도 이유를 모른다). **`Ballistics.Hash`는 이 검사를 하지 않는다** - 첫 인자가 stableId 전용이 아니라 `projectileId`(자체 카운터)도 리터럴 `0`(`CameraSystem`, `HullStructure.Breakaway`)도 온다. 검사는 `this`를 아는 호출자에 있고, 폴백은 `GetInstanceID()`다. Hash 안에서 early return하면 반환형이 `uint`라 **잘못된 판 전부가 같은 시드를 공유한다** - 결정론이 깨지는 게 아니라 *가짜로 성립해서* 두 번 돌려도 같은 결과가 나오고, 증상은 "파편이 다 같은 방향으로 튀는데"로 한참 뒤에 나온다. 그리고 **한 오브젝트의 `Thing` 전부에 찍는다** - `ThingDef.Spawn`은 `thingClass` 하나만 돌려주는데 `comps`에도 Thing이 올 수 있다(`Ballistic Door` = `BallisticArmor` + `Door`). 대가: def의 배치를 하나 지우면 그 뒤 전부가 밀린다. 어제 튜닝한 사격이 오늘 재현 안 되는 것은 버그가 아니라 이 설계의 가격표다.
- **def는 붙이는 순서가 아니라 켜는 순서가 중요하다** — `ThingDef.Spawn`은 GameObject를 **비활성으로 만들고**, 콜라이더·컴포넌트·stats·위치를 다 넣은 **뒤에** 켠다. `AddComponent`는 오브젝트가 활성이면 `Awake`를 즉시 부르기 때문에, 그냥 두면 `Armor.Awake`가 stats보다 먼저 돌아 모든 판이 기본값 체력으로 태어난다. 이 규칙 하나가 URP `Light2D` 같은 남의 컴포넌트까지 데이터로 설정 가능하게 만든다 — `OnEnable`이 값이 다 들어간 뒤에 돌아서 내부 캐시가 올바르게 잡힌다.
- **인스펙터 값은 def가 있으면 장식이다** — `shipDefName`이 채워진 배는 Awake에서 def의 수치가 인스펙터를 덮어쓴다. 플레이 중에 인스펙터에서 `drag`를 밀어봐야 다음 실행에 되돌아온다. 튜닝은 JSON에서 하고 `Tools > Defs > Reload`.
- **export는 배치만 다시 쓴다** — 함선 def에는 손으로 튜닝한 배 수치가 같이 들어 있다. `ShipDef.Save`가 통째로 직렬화하면 그게 조용히 사라지므로, `DefKeys.ReplaceTopLevelValue`로 `placements` 값만 갈아끼운다.
- **콜라이더는 def가 아니라 배치가 정할 수 있다** — `Placement.size`가 0이 아니면 def의 크기를 이긴다. 격자가 콜라이더를 안 보기 때문에 성립하는 것이고(위 "격자와 콜라이더는 다른 층이다"), 그래서 같은 def가 자리마다 다른 크기로 서도 방·선체·파단은 하나도 안 바뀐다. `hpPerSquareMetre`가 m²당이라 체력도 저절로 따라온다. `Placement.offset`은 **오브젝트가 아니라 콜라이더만** 민다 - `localPosition`이 곧 칸 번호라 그걸 밀면 `Stamp`가 다른 칸을 읽는다. 서브셀 격자도 `ArmorSkin`도 콜라이더 offset을 이미 읽으므로 셋이 같이 움직인다. 그리고 **그 값은 칸 좌표계지 판의 로컬이 아니다** - `BoxCollider2D.offset`은 회전 뒤의 로컬이라 `ThingDef.Spawn`이 `-rot`으로 돌려 넣고 `ShipExporter`가 `+rot`으로 되돌려 뽑는다. 안 돌리면 접선으로 세운 거울 판이 반지름 대신 원 둘레로 미끄러지고, 45° 경사판은 √2/2씩 샌다 - 증상이 "조금 어긋난다"뿐이라 눈으로는 회전 탓인지 offset 탓인지 안 갈린다.
- **클래스는 짧은 이름으로 부른다** — `DefKeys.Resolve`가 `Type.GetType` → 어셈블리별 정규화 이름 → **짧은 이름** 순으로 찾는다. 셋째가 없으면 URP `Light2D`를 붙이려고 `UnityEngine.Rendering.Universal.Light2D`를 통째로 적어야 하고, 남의 컴포넌트를 데이터로 붙일 수 있다는 이 설계의 값어치가 거기서 죽는다. **Component만 후보로 본다** - 여기 오는 이름은 전부 `thingClass` 아니면 `comps`라 아닌 것은 답이 될 수 없고, 이 한 줄이 후보를 수천에서 수십으로 줄인다. 그래도 겹치면 아무거나 안 고르고 거부한다(로드 순서에 결과가 달리면 기계마다 다르다). 이름에 점이 있는데 못 찾았으면 짧은 이름으로 안 내려간다 - 뒷부분만 떼어 비슷한 걸 집어오면 오타가 다른 클래스로 조용히 성공한다.
- **JsonUtility는 모르는 키를 조용히 버린다** — `rha`를 `rah`로 오타 내면 에러 없이 기본값이 들어가고 증상은 "장갑이 좀 약한 것 같은데"다. `ThingDef.Validate`가 리플렉션으로 (주 클래스 + comps 전부)의 직렬화 필드 이름을 모아 JSON 최상위 키와 대조하고, 하나라도 모르면 그 def를 아예 안 싣는다. 이 검증은 옵션이 아니라 데이터 주도 설계의 절반이다.
- **`hpPerSquareMetre`는 m²당이다** — 판의 총 구조 예산은 콜라이더 넓이를 곱해서 나온다. 총량으로 두면 0.4×1.0 얇은 패널이 1×1 벽과 같은 체력을 갖고, 45° 경사판(1×1.414)은 프리팹에 √2를 손으로 곱해 적어야 한다 - 새 판 모양마다 사람이 곱셈하는 규칙은 언젠가 반드시 잊힌다. `Armor.Awake`가 콜라이더를 HP 초기화보다 **먼저** 읽어야 하는 이유가 이것이다.
- **적열은 HP와 다른 정보다** — `Armor.Heat`는 시뮬레이션이 한 번도 안 읽는 시각 전용 값이다. HP는 "얼마나 상했나", 열은 "**언제** 상했나"를 말한다. 시뻘건 단면은 방금 찢어진 곳이고 검게 식은 잔해는 한참 전에 떨어진 것 - 시간 정보가 색만으로 전해진다. 그래서 열의 제일 큰 원천이 피해가 아니라 **이웃 판의 죽음**(`HeatFromExposure`)이다: 안쪽이던 면이 바깥이 된 순간이 곧 절단면이다.
- **열은 절대 피해가 아니라 서브셀 하나 기준 비율로 잰다** — `Armor.ApplyDamage`가 `amount / SubCellFullHp * HeatFromDamage`이고 그 분모는 판 HP의 1/36이다. 후면(`HullStructure.DamageRear`)도 같은 눈금을 써야 하는데, 서브셀이 없다고 칸 HP를 통째로 분모에 넣으면 **같은 피해가 36배 차갑게 나와 화면에서 0이 된다.** 실제로 그랬고 증상은 "뚫린 둘레만 타고 맞은 자리는 안 탄다"였다 - 상수를 의심하게 되지만(`HeatFromExposure`에 x10을 붙여도 안 변한다) 틀린 것은 분모다. 후면은 `maxHp / SubCount`를 단위로 빌려 쓴다. **앞뒤가 같은 눈금이 아니면 한쪽이 안 탄다.**
- **후면을 지우는 자리는 하나여야 한다** — `HullStructure.KillRear`가 달구고 지운다. 예전에는 `DamageRear`(갉아서 hp 0)와 `PunchRear`(관통)가 따로 지웠고, **극적인 쪽인 관통에만 열이 빠져 있었다** - 못 뚫고 갉는 것만 빛나고 뚫고 나가는 것은 조용한, 정확히 거꾸로 된 그림이다. 그리고 **지우기 전에 달궈야 한다**: 지운 뒤엔 `HeatRearAt`이 그 칸을 못 찾고, 못 찾으면 `RearHeatSpread` 표의 이웃 12칸도 안 돈다. 이 칸 자신의 열은 곧 사라지므로 화면에 남는 것은 둘레다 - 구멍은 검고 테두리가 탄다(셰이더가 `Torn` 칸을 아예 안 그린다).
- **그림의 주인은 판이면 배, 모듈이면 def다** — `ArmorSkin`은 선체 그림(`hullSkin`) 한 장에서 자기 조각을 굽는다. **판이 곧 선체이기 때문에** 그것이 맞다. 모듈은 선체가 아니라 볼트로 붙은 남이라 자기 def의 PNG(`skinTexture`)를 든다 - 엔진을 배 그림에 그려 넣으면 같은 엔진의 그림이 배 파일 열두 개로 흩어지고(lance는 엔진이 서른 개다), 배치를 한 칸 옮길 때마다 틀어지고, `Placement.size`로 크기를 덮어쓴 자리에서는 엉뚱한 픽셀이 나온다. "물건 한 종류가 파일 하나"가 그림에도 그대로 간다. 모듈 PNG는 **가로가 콜라이더와 정확히 같고 세로만 남는다** - 남는 세로가 포신이고, 그림의 아래 끝이 콜라이더의 아래 끝이라는 규칙 하나가 pivot을 정하므로 def에 pivot이 없다. 축척은 `ShipDef.PPU`(1 m = 96 px) 고정이라 배 그림과 같은 자 위에 선다. 안 맞으면 에러를 찍고 단색으로 떨어진다 - 조용히 늘려 쓰면 증상이 "좀 어긋난다"뿐이라 못 찾는다.
- **HDR은 `SpriteRenderer.color`를 못 넘는다** — 적열은 1을 넘는 색이라야 Bloom이 무는데, 그 값을 `_renderer.color`에 넣으면 1 초과분이 잘려서 문턱을 못 넘는다. 예전 CPU 굽기가 됐던 것은 텍스처 픽셀에 HDR을 **직접** 썼기 때문이고, 스킨이 셰이더로 넘어오면서 그 길이 사라졌다. 그래서 C#은 `heat`를 0~1 float 하나로만 `MaterialPropertyBlock`에 넣고, 1을 넘는 색은 셰이더의 `HeatTint`가 만든다(`PlateSkin`·`RearSkin` 두 벌이 같은 값이어야 뜯긴 단면의 앞뒤가 안 어긋난다). **그리고 `CombinedShapeLightShared` 뒤에 더한다** - 2D 라이트는 albedo를 곱하므로 어두운 자리에서는 아무리 큰 값을 넣어도 그 곱셈에서 도로 눌린다. 적열은 스스로 내는 빛이라 그 곱셈 밖에 있어야 한다. 증상이 **"색은 주황인데 안 빛난다" 하나뿐이라** 셰이더를 세 번 고치는 동안 원인이 안 보였다 - 색이 보이는 것이 곧 값이 도착했다는 뜻이라 데이터 경로를 의심하지 않게 된다. 이 규칙 덕에 **판마다 Light2D를 달지 않고도** 발광이 나온다. 실제 광원은 선체가 갈라지는 순간 단면에 하나만 놓는다.
- **판은 텍스처와 색을 따로 쓴다** — `ArmorSkin`의 텍스처는 *무엇이 부서졌나*(서브셀 HP), `heat`는 *얼마나 뜨거운가*다. 후면도 같은 열을 든다 - `RearCell`이 뜯기는 순간 앞판의 `Heat`를 물려받는다. 그 값은 매 틱 갱신하지 않고 **적은 틱에서 흐른 시간으로 감쇠를 계산한다**(`RearHeatNow`): 안 뜨거운 칸이 절대 다수라, 매 틱 도는 상태로 두면 판이 죽는 속도만큼 비용이 붙는다.
- **시타델은 규칙이 아니라 배치다** — 탄약고·원자로(`CriticalModule`)는 격자에 새긴 구역이 아니라 판에 볼트로 붙는 모듈이고, 어디에 붙였는지가 곧 결과다. destroyer로 재보면 같은 `blastDamage 1600`이 **중앙·상부구조 밖**이면 선체를 143 + 45로 가르고(T-80), **선수**면 44칸만 날리고 배는 살고(에이브럼스), **상부구조 밑**이면 판을 제일 많이 죽이는데도 함교 지붕이 다리를 놓아 안 갈라진다. 분기문은 없다.
- **유폭은 등방성 충각이다** — `RamImpact.Conduct`에 `along == across`를 주면 띠가 아니라 원이 된다. "폭발" 시스템이 따로 없다. 반경을 정하는 것은 `blastDamage`가 아니라 `BlastCutoff`이고, 세기는 "닿은 판이 죽느냐"만 정한다.
- **유폭의 매질은 둘이고 몸 경계로 갈린다** — 자기 몸은 `Conduct`가 `Armor.Neighbours` 그래프로 타고(이미 뚫린 구멍에서 끊긴다), 남의 몸은 `Radiate`가 `OverlapCircle` 한 번으로 건너간다(거리만 본다). 그래프로는 적함에 못 닿는다 — `Neighbours`는 자기 배 격자에서 채워지므로 적함 판이 애초에 들어 있지 않고, `SameBodyAs`를 빼도 잔해 참조만 되살아난다. 반경은 손으로 적지 않는다: `Ballistics.BlastRadius`가 `BlastFalloff^r == BlastCutoff`를 푼 값이라 튜닝을 따라온다.
- **유폭은 자기 자신 한가운데서 재진입한다** — `Armor.Die` → `CollapseRemains` → `SpallResolver.Burst` → 파편이 다른 탄약고 명중 → `CriticalModule.TakeDamage` → `Detonate`가 **전부 동기다.** 그래서 `Conduct`/`Radiate`의 정적 버퍼(`_wave`·`_reached`·`_nearby`)를 그냥 쓰면 안쪽 폭발이 바깥 폭발의 큐를 비우고, 바깥 `while`이 빈 큐를 보고 조용히 끝난다 — 제일 큰 폭발이 제일 적게 번진다. `_conducting`/`_radiating` 깊이 카운터가 재진입일 때만 지역 버퍼로 갈아탄다. `MaxDetonationChain`은 깊이만 막지 이 공유 상태는 못 막는다. 같은 이유로 `Radiate`는 `_reached`가 아니라 `SameBodyAs`로 자기 몸을 거른다 — 공유 상태를 안 읽으면 그 창이 존재하지 않는다.
- **"지금 없다"와 "애초에 없었다"는 다른 말이다** — `Ship.HasPower`가 살아 있는 원자로를 세는데, 터진 원자로는 판과 함께 잔해로 떠나거나 파괴돼서 **목록에서 사라진다.** 남은 것만 보고 "원자로가 하나도 없네 = 전기 걱정 없는 설계"로 떨어지면, 원자로가 전멸한 배가 계속 조타하고 조준한다. 설계에 원자로가 있었는지는 `Ship.Awake`가 `_needsPower`에 한 번 적고 안 바꾼다. `Cell.Unset`이 0이어야 하는 것과 같은 이유다 - 기본값이 두 상태를 삼키면 그 구분이 영영 안 돌아온다.
- **준비 플래그 셋은 파생값이다** — `isDriverReady`/`isGunnerReady`/`isEngineerReady`는 저장하지 않는다. 예전에는 `Crew()`가 끄는 public bool이었는데, 원자로까지 끄게 하면 주인이 둘이 되고 원자로 복구 순간 죽은 승무원이 되살아난다. 조건을 읽는 자리가 하나면 그 버그가 존재할 자리가 없다.
- **파편 연쇄 상한 2개** — `MaxSpallDepth`(레이), `MaxFragmentGeneration`(실체 파편). 둘 다 없으면 한 발이 함선을 지운다.
- **첫 승리의 반쪽은 정상이다** — `RunState.Save`는 배 파일만 쓰고 진행도 파일은 `Salvage`/`Sector` setter가 처음 불릴 때 만든다. 그래서 승리 직후 한쪽만 있는 창이 **정상 경로에 항상 열린다.** 그 창을 손상으로 보고 지우면 첫 노획이 영영 0이 되고 배 저장까지 같이 날아가는데, 로그는 "승리"와 "노획 N장"을 멀쩡히 찍는다 - 두 번째 판부터는 진행도 파일이 이미 있어서 재현도 안 된다. 그래서 판정(`Exists`, 순수 함수)과 정리(`ValidateOrClear`, 파일을 지운다)의 주인이 갈려 있어야 하고, 정리는 **런이 시작될 때 한 번**뿐이다. 같은 함수를 `Begin()`처럼 구역마다 도는 자리에서 부르면 정상 상태를 오진한다 - 시작 시점의 반쪽은 "지난 세션이 두 쓰기 사이에서 끊겼다"이고 런 도중의 반쪽은 "지금 쓰는 중이다"라, **부르는 시점이 같은 검사의 의미를 바꾼다.**
- **`Battle`은 MonoBehaviour가 아니다** — 필요한 것이 술어 하나와 걸쇠 둘뿐이라 `Campaign`이 필드로 들고 자기 `OnTick`에서 부른다. 컴포넌트였을 때 딸려온 문제가 셋이었다: 씬에 손으로 놓을 수 있어서 `Campaign.Begin`의 `AddComponent`와 겹치면 `onAnyEnd`가 두 번 발화해 **구역이 두 칸 넘어가고 노획이 두 배로 들어갔고**(`Ended` 걸쇠는 인스턴스 필드라 다른 인스턴스를 못 막는다), 생성·파괴가 `TickManager._listeners` 순회 도중에 일어났고, 구독자가 Battle보다 먼저 사는 탓에 `onAnyEnd`가 static이어야 했다. 막간(`interludeTicks`)은 이제 안전장치가 아니라 읽힘을 위한 것이다.

## 파일 지도

```
Ballistics.Tuning.cs      상수 전부. 숫자 만질 일이면 여기만.
Ballistics.Formula.cs     관통 공식, RHA 곡선
Ballistics.SubCell.cs     판 안쪽 6×6 격자, 다중 레인 채널
RamImpact.cs              충각 피해 + 충격 전도. 비등방=절단, 등방=유폭. 둘 다 여기서 자연발생한다
CriticalModule.cs         탄약고·원자로. 죽으면 터진다. 둘의 차이는 데이터 두 개뿐
PenetrationManager.cs     판정. 순수 함수, RNG 없음, 바깥을 안 바꾼다
Projectile.cs             시간 예산 틱 루프
Projectile.Surfaces.cs    레이캐스트 → 접촉면 수집
Projectile.Damage.cs      결과 적용. 바깥이 바뀌는 유일한 자리
Ship.Split.cs             선체 연결성 BFS, 파단
Ship.Ram.cs               충각 → 기존 국부 손상 경로
Hulk.cs                   배 아닌 떠다니는 덩어리. 잔해·운석·거울·폐위성이 전부 이것
ShipGrid.cs               1m 정수 격자. 위상만 - 콜라이더는 여기 안 온다
ShipBuilder.cs            배치 리스트 ↔ 자식 오브젝트. 양방향이 한 파일에 있어야 왕복이 닫힌다
ShipExporter.cs           씬 → JSON (에디터). 뽑고 나서 왕복 검증까지 돈다
ShipDef.cs                배 한 척: 배치 리스트 + 배 수치. StreamingAssets/Ships/*.json
DefKeys.cs                def 키 검증·타입 해석·비파괴 JSON 병합. ThingDef와 ShipDef가 공유
RoomView.cs               방 기압 오버레이. 새는 방을 따로 칠한다 (Tab으로 토글)
ThingDef.cs               물건 한 종류의 정의 + 검증 + Spawn
DefDatabase.cs            defName → ThingDef. StreamingAssets/Defs를 훑는다
SolidSkin.cs              모듈·탄의 그림. def의 PNG 한 장 (ArmorSkin은 판 = 선체 그림)
ModuleSkinTemplate.cs     그 PNG의 밑판을 def마다 뽑는다 (에디터). 회전 중심·포구 끝 표시
ImGui.cs                  즉시 모드 선언 층. 안 부르는 것이 곧 지우는 것
GUIItemData.cs            GUIItem과 위젯 종류들. Decorative가 입력 판정에서 빠진다
StoryScriptManager.cs     대사창 + 대본. StreamingAssets/대사/*.json (작성법은 그 폴더의 README)
RunLog.cs                 이번 런에 일어난 일. 사건의 단일 깔때기 - UI가 여기를 구독한다
Battle.cs                 전투가 끝나는 순간. 종료 조건은 술어 필드라 목표가 늘어도 틱 루프는 그대로
RunState.cs               런의 손상 저장. 손상된 배 = 배치가 적은 ShipDef
```

## 설계도

배는 JSON 배치 리스트로 짓는다. 텍스트 맵은 죽었다 (`ShipGrid.ParseMap`은 테스트 픽스처 전용).

- 저작은 씬에서, 결과는 `Tools > Ship > Export Selected Ship To Json`으로 뽑는다.
- `Ship.shipDefName`이 비어 있으면 씬의 자식을 그대로 쓴다(= export 원본). 채워져 있으면 Awake에서 자식을 갈아엎고 JSON대로 짓는다. 두 원본을 동시에 살려두면 반드시 어긋난다.
- **`Ship`은 abstract가 아니다.** 함선의 종류는 C# 클래스가 아니라 `shipDefName`이다. `ShipDef`는 배치 리스트 **그리고** 배 수치(`drag`, `angleAccel`, `FightDistance`...)를 함께 들고, ThingDef와 같은 두 번 읽기로 Ship에 붓는다.
- **`team`은 설계가 아니다.** 같은 구축함이 아군일 수도 적군일 수도 있다. 위치·좌우 반전(`localScale.x`)도 같은 이유로 def에 없다 — 소환 인자다.
- 질량은 판 수 × `massPerPlate`. 세 척에 같은 숫자를 손으로 적어두면 작은 배가 큰 배만큼 굼떠진다.
- **프리팹은 없다.** 물건 한 종류가 `StreamingAssets/Defs/<이름>.json` 파일 하나다 — 어느 C# 클래스를 붙일지(`thingClass`), 어떤 부속을 같이 달지(`comps`), 콜라이더가 얼마인지, 수치가 얼마인지 전부.
- 그림도 자산이 아니다. 스프라이트 파일이 없고 `ArmorSkin`(판. 텍스처를 굽지 않는다 - 배 그림 + 6×6 손상 마스크 + 실루엣 마스크를 `PlateSkin` 셰이더가 조합하고, CPU는 피해 시 마스크 36바이트만 쓴다)과 `SolidSkin`(모듈·탄, def의 PNG)이 콜라이더에서 런타임에 만든다. **def의 색이 곧 아트다.** 서브셀·실물 모양 수식의 원본은 C# 한 벌(`Ballistics.SubIndex`, `Armor.InsideShape`)이고 셰이더는 상수와 구운 마스크를 받아 조합만 한다.
- def는 서로를 **이름으로만** 안다 (`Gun.projectile: "Railgun Bullet"`). JSON끼리는 GUID가 없으니 그것뿐이고, 그게 모딩이 열리는 지점이다.

## 테스트

`Tools > Ballistics > Run Penetration Tests` (에디터 메뉴). 씬도 플레이 모드도 필요 없다 —
`Resolve`가 순수 함수라서. 탄도 수식이나 서브셀 격자를 건드렸으면 돌릴 것.

## Skill routing

When the user's request matches an available skill, invoke it via the Skill tool. When in doubt, invoke the skill.

Key routing rules:
- Product ideas/brainstorming → invoke /office-hours
- Strategy/scope → invoke /plan-ceo-review
- Architecture → invoke /plan-eng-review
- Design system/plan review → invoke /design-consultation or /plan-design-review
- Full review pipeline → invoke /autoplan
- Bugs/errors → invoke /investigate
- QA/testing site behavior → invoke /qa or /qa-only
- Code review/diff check → invoke /review
- Visual polish → invoke /design-review
- Ship/deploy/PR → invoke /ship or /land-and-deploy
- Save progress → invoke /context-save
- Resume context → invoke /context-restore
- Author a backlog-ready spec/issue → invoke /spec
