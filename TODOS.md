# TODOS

미룬 것들. 이유와 배경을 같이 적는다 — 맥락 없는 한 줄짜리 TODO는 없느니만 못하다.

## 잔해도 쪼개져야 한다

**무엇** — `HullDebris`가 한 번 떨어져 나온 뒤로는 다시 갈라지지 않는다. 3장짜리 잔해의
가운데 판을 쏴서 없애도, 남은 두 판이 서로 안 닿는데 하나의 리지드바디로 붙어 다닌다.

**왜 지금 안 하나** — `Ship.SplitIfBroken`은 칸 좌표 격자 위에서 8방향 BFS를 돈다.
`Breakaway`가 판을 새 GameObject로 재부모화하면서 `localPosition`이 바뀌어 그 격자가
사라진다. 잔해에 같은 BFS를 돌리려면 격자를 들고 다니거나, 다른 방법이 필요하다.

**어떻게 할 것인가** — 격자를 들고 다니지 말고 **기하로 잇는다.** 재부모화 뒤에도 판들은
여전히 같은 1 m 격자 위에 있으므로, 두 판의 중심 거리가 `1.5 * ShipGrid.CellSize` 이내면
이웃으로 친다(대각 1.41까지 포함, 8방향과 같은 결과). 이러면 잔해가 격자 없이도 쪼개지고,
같은 함수를 나중에 함선 쪽에도 쓸 수 있다.

**의존** — 없음. 판 하나짜리 잔해가 흔해지면 그때가 할 때다.

## 함선 맵을 JSON으로

**무엇** — 지금 텍스트 맵(`Assets/Data/Ships/*.txt`)이 칸당 문자 하나로 벽/문/엔진만 정한다.
JSON으로 바꿔서 판마다 각도와 크기를 지정할 수 있게 한다.

**왜** — 서브셀 격자가 콜라이더 크기를 그대로 따라가므로, 판을 기울이고 늘리면 장갑 해상도와
경사 장갑이 공짜로 따라온다. 텍스트 맵의 격자 한 칸 = 판 하나 = 1×1 m 고정이 그걸 막고 있다.

**절대 건드리지 않을 것** — **논리 격자는 그대로 둔다.** 방 구획·기압·파공·선체 연결성 BFS가
전부 칸 좌표 위에서 돈다. 격자를 손대면 그게 다 깨진다. JSON이 옮기는 것은 **콜라이더와
스프라이트뿐**이고, 그 판이 논리적으로 어느 칸인지는 변하지 않는다.

**유일한 진짜 설계 결정** — `Ship.cs`의 `BuildRooms()`가 판의 칸을 위치에서 역산한다:

```csharp
armorAt[ShipGrid.ToCell(armor.transform.localPosition, mapWidth, mapHeight)] = armor;
```

판이 칸 중심을 벗어나는 순간 이 역산이 틀린다. JSON은 칸 좌표를 **명시적으로** 들고 있어야
하고, 그걸 판에 실어 `BuildRooms`가 위치 대신 그 값을 읽어야 한다. 여기만 정하면 나머지는
따라온다.

**이미 되어 있는 것 (다시 만들지 말 것)**
- 회전: `Ballistics.SubCell.cs`의 계산이 전부 판의 로컬 공간이라 기울어진 판에 특수 처리가 없다
- 가변 크기: `Armor._cellSize`를 `BoxCollider2D.size`에서 읽으므로 서브셀이 이미 콜라이더를 따라간다
- 외형: `ArmorSkin`이 콜라이더 모양에서 스프라이트를 굽는다. 자동으로 따라온다

**의존** — 없음. 지금 해도 되고, 다만 적 함선이 생겨서 튜닝 피드백이 도는 뒤가 낫다.
